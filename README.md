# 🎬 ZENFLIX — Nonton Film & Series Streaming Premium

[![Next.js](https://img.shields.io/badge/Next.js%2016-black?style=flat&logo=next.js)](https://nextjs.org)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?style=flat&logo=typescript)](https://typescriptlang.org)
[![Tailwind](https://img.shields.io/badge/Tailwind%20CSS%204-38bdf8?style=flat&logo=tailwindcss)](https://tailwindcss.com)
[![Supabase](https://img.shields.io/badge/Supabase-3ECF8E?style=flat&logo=supabase)](https://supabase.com)
[![Vercel](https://img.shields.io/badge/Deployed%20on-Vercel-black?style=flat&logo=vercel)](https://vercel.com)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

> **Stream the best movies & series in stunning HD, zero buffering.**
> Platform streaming film dengan UI modern, fitur lengkap, dan player HD dengan kualitas pilihan.

---

## ✨ Fitur

| Fitur | Deskripsi |
|-------|-----------|
| 🎥 | **Trending, Now Playing, Popular, Top Rated** — 4 kategori film real-time dari TMDB |
| 🔍 | **Live Search Dropdown** — cari film instan dengan debounce + autocomplete |
| 📺 | **TV Series + Season/Episode Picker** — episode selector dengan pagination 10/page |
| ▶️ | **Player HD (HLS)** — player custom dengan pilihan kualitas (1080p/720p/480p/auto) |
| 🎬 | **IDLIX Stream Source** — self-hosted HLS stream + subtitle Indonesia (via Docker) |
| ❤️ | **Favorites & Watchlist** — simpan film kesukaan per user (Supabase) |
| 💬 | **Komentar** — diskusi per film dengan akun login |
| 🔐 | **Supabase Auth** — register/login dengan session persistence + Google OAuth |
| 💳 | **Midtrans Payment** — subscription Remove Ads Rp20rb via Snap popup |
| ⏱️ | **Auto-logout 1 jam inactive** — keamanan session |
| 🎨 | **Glassmorphism UI** — dark theme, efek kaca, gradient text |
| 📱 | **PWA Installable** — install di mobile, offline-ready (Service Worker) |
| 🐳 | **Docker** — containerized, deploy cepat |
| 📊 | **Vercel Analytics** — pantau pengunjung & page views |

---

## 🚀 Quick Start

### Prerequisites
- Node.js **20+**
- **TMDB API Key** — [daftar gratis di sini](https://www.themoviedb.org/settings/api)
- **Supabase** — [buat proyek gratis](https://supabase.com)
- **Midtrans** — [daftar sandbox](https://midtrans.com)

### 1. Clone & Install
```bash
git clone https://github.com/Alleyaaa/ZENFLIX.git
cd ZENFLIX
npm install
```

### 2. Environment Variables
Buat file `.env` di root project:
```env
# ─── TMDB (required) ───
TMDB_API_KEY=your_tmdb_api_key_here

# ─── Supabase (required) ───
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_KEY=your_service_role_key_here

# ─── Midtrans (optional, utk payment) ───
MIDTRANS_SERVER_KEY=your_midtrans_server_key_here
MIDTRANS_CLIENT_KEY=your_midtrans_client_key_here
```

### 3. Setup Supabase (SQL Editor)
Jalankan SQL berikut untuk tabel favorites, watchlist, comments, dan bug_reports:
```sql
-- Favorites table
CREATE TABLE IF NOT EXISTS public.favorites (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  media_type text DEFAULT 'movie',
  tmdb_id bigint NOT NULL,
  title text,
  poster_url text,
  rating numeric,
  year text,
  created_at timestamptz DEFAULT now()
);

-- Watchlist table
CREATE TABLE IF NOT EXISTS public.watchlist (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  media_type text DEFAULT 'movie',
  tmdb_id bigint NOT NULL,
  title text,
  poster_url text,
  rating numeric,
  year text,
  created_at timestamptz DEFAULT now()
);

-- Comments table
CREATE TABLE IF NOT EXISTS public.comments (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  media_type text DEFAULT 'movie',
  tmdb_id bigint NOT NULL,
  content text NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- Bug reports table (admin)
CREATE TABLE IF NOT EXISTS public.bug_reports (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  type text DEFAULT 'bug',
  title text NOT NULL,
  description text NOT NULL,
  priority text DEFAULT 'medium',
  status text DEFAULT 'new',
  steps text,
  browser text,
  device text,
  user_id uuid,
  anonymous boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);
```

> ⚠️ Aktifkan **Row Level Security** di tiap tabel:
> ```sql
> ALTER TABLE public.favorites ENABLE ROW LEVEL SECURITY;
> CREATE POLICY "own_favorites" ON public.favorites FOR ALL USING (auth.uid() = user_id);
> ALTER TABLE public.watchlist ENABLE ROW LEVEL SECURITY;
> CREATE POLICY "own_watchlist" ON public.watchlist FOR ALL USING (auth.uid() = user_id);
> ALTER TABLE public.comments ENABLE ROW LEVEL SECURITY;
> CREATE POLICY "read_comments" ON public.comments FOR SELECT USING (true);
> CREATE POLICY "write_comments" ON public.comments FOR INSERT WITH CHECK (auth.uid() = user_id);
> ```

### 4. Jalankan
```bash
npm run dev
# buka http://localhost:3000
```

### 5. Deploy ke Vercel
```bash
npx vercel --prod
```
Set environment variables di Vercel → Settings → Environment Variables.

---

## 🎯 IDLIX Stream (Opsional — Kualitas HD + Subtitle ID)

IDLIX menyediakan stream self-hosted (bukan embed Vidsrc) dengan kualitas stabil + subtitle Indonesia. Jalankan via Docker:

```bash
# 1. Clone source
git clone https://github.com/annurdien/IDLIX-API.git
git clone https://github.com/annurdien/stealth.git

# 2. Build & run (docker-compose.local.yml di repo ini)
docker compose -f docker-compose.local.yml up -d --build

# 3. Set env di Vercel
# IDLIX_API_URL=https://your-idlix-host:3000
```

> ⚠️ IDLIX API butuh host publik (Render/Railway/VM) supaya Vercel bisa akses.
> Tanpa itu, player otomatis fallback ke resolver HLS Vidsrc.

---

## 🏗️ Arsitektur

```
zenflix/
├── src/
│   ├── app/
│   │   ├── api/                 # API Routes (server-side)
│   │   │   ├── idlix/           # IDLIX stream proxy (self-hosted HLS)
│   │   │   ├── resolve-hls/     # Resolver HLS (kualitas manual)
│   │   │   ├── stream-proxy/    # Proxy stream (source tersembunyi)
│   │   │   ├── stream-assets/   # Proxy asset source
│   │   │   ├── sources/         # Channel picker
│   │   │   ├── subscription/    # Midtrans payment
│   │   │   ├── contact/         # Laporan bug
│   │   │   ├── watchlist/       # Watchlist CRUD
│   │   │   └── ...
│   │   ├── movie/[id]/          # Detail film + player
│   │   ├── tv/[id]/             # Detail series + episode picker
│   │   ├── admin/               # Panel admin
│   │   ├── contact/             # Kontak & lapor bug
│   │   └── ...
│   ├── components/
│   │   ├── HlsPlayer.tsx        # Player HLS custom (kualitas pilihan)
│   │   ├── PlayerSection.tsx    # Mode player (HD / Embed)
│   │   ├── CommentsSection.tsx  # Komentar per film
│   │   ├── DetailActions.tsx    # Favorite & Watchlist
│   │   ├── AdsterraAds.tsx      # Ads (popunder, banner, native)
│   │   └── ...
│   └── lib/
│       ├── supabase.ts          # Supabase client
│       ├── tmdb.ts              # TMDB helpers
│       └── cache.ts             # API cache (memory + Redis-ready)
├── public/
│   ├── sw.js                    # Service Worker (PWA offline)
│   ├── manifest.webmanifest     # PWA manifest
│   └── icons/                   # PWA icons
├── e2e/                         # Playwright E2E tests
└── tests/                       # Jest unit tests
```

---

## 📋 Progress

### ✅ Sudah Berfungsi
- [x] Homepage dengan hero + movie rows
- [x] Live search dropdown dengan debounce
- [x] Detail film: sinopsis, cast, rating, similar movies
- [x] TV Series: season/episode picker + player
- [x] Player HD Custom (HLS.js) — pilih kualitas manual
- [x] Supabase auth: register, login, Google OAuth, session persistence
- [x] Auto-logout setelah 1 jam inactive
- [x] Genre & category browsing
- [x] Glassmorphism UI (merata di seluruh halaman)
- [x] Favorites & Watchlist (per user, sync ke profile)
- [x] Komentar per film/series (login-only)
- [x] Subscription: Gratis vs Remove Ads (Rp20rb) via Midtrans
- [x] PWA installable (manifest + service worker + offline)
- [x] Vercel Analytics (pantau pengunjung)
- [x] Docker containerized
- [x] Lazy loading poster & skeleton loading
- [x] Panel Admin (/admin)
- [x] Halaman Kontak & Lapor Bug (/contact)
- [x] Ads Adsterra (popunder, socialbar, banner 300x250, native)

### ⏳ Dalam Pengerjaan
- [ ] IDLIX stream di production (butuh host publik)
- [ ] Email verification & forgot password flow (SMTP)
- [ ] Watch history & continue watching
- [ ] User profile page (avatar, username, ganti password)
- [ ] PWA push notifications
- [ ] Redis caching (Upstash)

---

## 🛠️ Tech Stack

| Teknologi | Fungsi |
|-----------|--------|
| Next.js 16 (Turbopack) | Framework React |
| TypeScript | Type safety |
| Tailwind CSS 4 | Utility-first CSS |
| Supabase | Auth + Database (Postgres) |
| TMDB | Movie/TV data |
| hls.js | HLS player custom |
| Midtrans | Payment gateway |
| Docker | Containerization |
| Vercel | Deploy + Analytics |
| Playwright | E2E testing |
| Jest | Unit testing |

---

## 🤝 Kontribusi

Pull requests & issue sangat diterima! Untuk perubahan besar, buka issue dulu ya.

1. Fork repo
2. Buat branch fitur: `git checkout -b feat/feature-name`
3. Commit: `git commit -m 'feat: nambah fitur keren'`
4. Push & buat PR

---

## 📄 Lisensi

MIT © [Alleyaaa](https://github.com/Alleyaaa) — bebas pakai, fork, & modifikasi.

---

**⭐ Jangan lupa star repo ini & traktir kopi di [Trakteer](https://trakteer.id/sugiono_nur_aceng/tip) ya!**